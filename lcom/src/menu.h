#pragma once

/*
 * @brief Draws the menu
 * @return Returns 1 if there was not an error
 */
int draw_menu();

/*
 * @brief Draws a menu of game status
 */
void draw_init_multi(bool mode);

/*
 * @brief Prints how the gama ended
 * @return Returns 1 if there was not an error
 */
int print_winner();


