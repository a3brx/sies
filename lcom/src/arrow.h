#pragma once

/*
 * @brief This functions deals with printing the mouse and calls the functions that make a move or print the possible moves of a piece if the left mouse button was pressed
 * @return Returns 0 if there was no error
 */
int print_mouse();

